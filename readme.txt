1) Use webdriver to open a sudoku website
2) Parse through the html/script page to get the 81-digit string
3) Return that string to main.py which uses backtracking algorithm to solve the puzzle and subsequently return the solved string
4) Selenium in output.py uses this string to output the numbers to the page
